import Section from '../section'

const subtitle = `Le cadastre est un ensemble de plans et fichiers administratifs qui recensent
toutes les propriétés immobilières situées dans chaque commune française, et qui en consigne
la valeur afin de servir de base de calcul à certains impôts. (Wikipedia)`

export default () => (
  <Section title='Qu’est-ce que le cadastre ?' subtitle={subtitle} />
)
